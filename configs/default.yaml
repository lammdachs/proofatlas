# Default configuration for ProofAtlas
# This file includes the core configuration and can be extended

# Include core configuration
include:
  - core.yaml

# Override any core settings here if needed
# For example:
# proving:
#   max_clauses: 5000

# To use ML features, also include ml.yaml:
# include:
#   - core.yaml
#   - ml.yaml